export interface loginResponseDTO {
  firstName: string;
  lastName: string;
  accountId: string;
  middleName: string;
  phoneNumber: string;
  userId: string;
  email: string;
  accessToken: string;
  refreshToken: string;
}
